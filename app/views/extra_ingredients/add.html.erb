<% provide(:title, 'Add extra ingredients') %>
<h1>Add extra ingredients</h1> 

<div>
  <%= @product.title %>'s ingredients: 
  <ul>
    <% @product.ingredients.each do |item| %>
      <li> <%= item.name %> </li>
    <% end %>
  </ul>
</div>

<div>
  Add:
  <%= render 'form' %>
</div>
